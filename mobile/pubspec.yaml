name: pay_808
description: Flutter mobile app for 808Pay - Algorand-based payment settlement.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  # QR Code
  qr_flutter: ^4.0.0
  image_picker: ^1.0.0
  
  # Real-time QR Scanner
  mobile_scanner: ^3.5.0

  # HTTP Client
  http: ^1.0.0

  # State Management
  provider: ^6.0.0

  # Local Storage
  shared_preferences: ^2.0.0

  # Date Formatting
  intl: ^0.19.0

  # Icons
  cupertino_icons: ^1.0.0

  # Environment variables
  flutter_dotenv: ^5.0.0

  # Cryptography
  cryptography: ^2.1.0

  # Share functionality
  share_plus: ^7.0.0

  # URL Launcher
  url_launcher: ^6.1.0

  # Connectivity
  connectivity_plus: ^5.0.0
  
  # NFC - Tap to transfer
  nfc_manager: ^3.2.0
  
  # Offline transaction queue storage
  hive: ^2.0.0
  hive_flutter: ^1.0.0
  
  # Algorand SDK utilities (for transaction formatting)
  algorand_dart: ^1.0.3

  # Pera Wallet (via deep links)
  # Integration uses Pera's native deep link protocol

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true

  # Uncomment and add custom fonts here
  # fonts:
  #   - family: CustomFont
  #     fonts:
  #       - asset: assets/fonts/CustomFont.ttf
